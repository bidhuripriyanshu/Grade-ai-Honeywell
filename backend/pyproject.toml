[project]
name = "paper-factory-ai-backend"
version = "0.1.0"
description = "Paper Factory AI - Honeywell Backend API"
requires-python = ">=3.10"
dependencies = [
    "numpy",
    "joblib",
    "fastapi",
    "uvicorn",
    "sqlalchemy",
    "psycopg2-binary",
    "python-dotenv",
    "groq"
]

[tool.vercel]
entrypoint = "api.main:app"
