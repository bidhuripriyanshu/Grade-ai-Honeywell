[project]
name = "paper-factory-ai-monorepo"
version = "0.1.0"
description = "Paper Factory AI - Honeywell Platform"
requires-python = ">=3.10"
dependencies = [
    "numpy",
    "joblib",
    "fastapi",
    "uvicorn",
    "sqlalchemy",
    "psycopg2-binary",
    "python-dotenv",
    "groq"
]

[tool.vercel]
entrypoint = "backend.api.main:app"
